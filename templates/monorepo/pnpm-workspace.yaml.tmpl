---
variables:
  app_paths: "Glob patterns for app directories (e.g., apps/*)"
  package_paths: "Glob patterns for shared package directories (e.g., packages/*)"
---

packages:
  - "{{app_paths}}"
  - "{{package_paths}}"
